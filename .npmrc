# Expo's Metro bundler does not handle pnpm's default symlinked node_modules
# reliably. Hoisting makes resolution behave like npm/yarn, which Metro expects.
node-linker=hoisted

# React Native / Expo ship loose peer ranges; don't fail installs over them.
strict-peer-dependencies=false
auto-install-peers=true
